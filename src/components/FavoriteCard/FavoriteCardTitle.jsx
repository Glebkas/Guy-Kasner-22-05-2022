import styled from 'styled-components';

export const FavoriteCardTitle = styled.p`
    margin: 0;
    padding: 0;
    font-size: var(--fs-dsbg);
    font-weight: var(--fw-bold);
    text-align: center;
`;
