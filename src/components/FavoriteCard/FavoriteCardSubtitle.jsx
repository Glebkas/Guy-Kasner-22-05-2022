import styled from 'styled-components';

export const FavoriteCardSubtitle = styled.p`
    margin: 0.5rem 0 0 0;
    padding: 0;
    text-align: center;
    font-size: var(--fs-dssm);
    font-weight:var(--fw-bold);
`;
