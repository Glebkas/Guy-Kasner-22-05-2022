import styled from 'styled-components';

export const ForcastContainer = styled.section`
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
`;
